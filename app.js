const arr = [1, 2, 3, 4];
const str = "Good Morning Y'all";
const boolfool = true;

const testme = (a, b) => {
    a = arr;
    b = str;
    if(boolfool) {
console.log(`${a} is an array, followed by a string: ${b}`)
    };
};

testme();


